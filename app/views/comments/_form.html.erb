<div class="container">
  <div class="row">
    <div class="col-md-5 m-auto">
    
      <%= form_for([@space, @space.comments.new], remote: true) do |f| %>
      
        <div class="field form-group p-2">
          <%= f.text_area :content, class: "form-control p-3", placeholder: "Qu'avez-vous pensé de cet espace ?" %>
        </div>

        <div class="text-center mt-5">
          <% if user_signed_in? %>
            <%= f.submit "Poster", class: "my-btn-secondary" %>
          <% else %>
            <%= link_to "Poster", new_user_session_path, class: "my-btn-secondary" %>
          <% end %>
        </div>
      <% end %>

    </div>
  </div>
</div>